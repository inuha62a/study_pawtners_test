<turbo-stream target="comment_<%= @comment.id %>" action="replace">
  <template>
    <%= render partial: "comments/comment", formats: [:html], locals: { comment: @comment } %>
  </template>
</turbo-stream>