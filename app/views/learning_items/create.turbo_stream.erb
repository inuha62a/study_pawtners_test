<%= turbo_stream.replace "flash", partial: "shared/flash" %>

<% if @status == "incomplete" %>
  <%= turbo_stream.prepend "learning_items_list", partial: "learning_items/item", locals: { learning_item: @learning_item, status: @status } %>
<% end %>

<%= turbo_stream.update "new_learning_item_form", partial: "learning_items/form", locals: { learning_item: LearningItem.new, status: @status } %>
