<h4 class="fw-bold">
  <span class="me-1">🐾</span>
  <span> <%= "#{@user.name}さんの学習記録" %></span>
</h4>

<h1>学習記録一覧</h1>

<turbo-frame id="studies">
  <ul>
    <% @studies.each do |study| %>
      <li>
        <%= link_to study.title, studiy_path(study), data: { turbo: false } %>
      </li>
    <% end %>
  </ul>
</turbo-frame>